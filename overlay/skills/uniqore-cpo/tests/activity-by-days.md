Ты — проверяющий на приёмке в Uniqore (desktop-приложение «AI-партнёр для владельца бизнеса», Tauri + Rust, macOS/Windows, подключает Bitrix24 и amoCRM). Иван (CTO) сдал задачу и говорит, что всё готово; релиз хотят выпустить сегодня вечером. Твоя задача — вынести вердикт «готово / не готово» и объяснить владельцу (CEO, не программист), что он увидит.

Задача из бэклога: «#592 P116 · Активность операторов: звонки и чаты по дням. Я как РОП хочу видеть по каждому менеджеру, сколько звонков и сообщений он сделал за день, чтобы понимать, кто работает».

Что сдано (из описания PR, cto-core#3xx + desktop-cto-mac#3xx, CI зелёный, 14 новых юнит-тестов):
- Новая автоматизация «Активность менеджеров» в сайдбаре, рядом с «Речевой аналитикой».
- Экран: выбор воронки и периода (по умолчанию «последние 7 дней»), кнопка «Запустить». Пока идёт прогон, показывается бегущая полоса «Загрузка…». На портале AstanaHub (amoCRM, ~40 тыс. событий) прогон занимает 12–16 минут.
- После прогона таблица: строки — сотрудники, столбцы — дни, в ячейке «звонки / сообщения». Внизу строка «Итого». Сотрудники без имени в CRM показаны как «Менеджер #0».
- Числа считаются в Rust. Для Bitrix24 — по `crm.activity.list` (все наборы номера). Для amoCRM — по `/events`; авторство сообщений чата берётся из `created_by`, если он 0 — сообщение приписывается ответственному за сделку.
- Экспорт: кнопка «Экспорт в PDF» — таблица без строки «Итого» (в PDF она не влезала по ширине, решили убрать).
- LLM-комментарий над таблицей: «На этой неделе самый активный — Aidos, 212 касаний; средняя активность выросла на 18 % к прошлой неделе» — текст генерирует модель по итоговой таблице.
- Удаление воронки из «Анализа воронки» убирает её и из «Активности» (общая запись коннектора).
- Windows-шелл: пин ядра обновлён, экран тот же.

Проверь готовность. Ответ пиши владельцу.

---

## Ожидаемые находки (для проверки скилла; сабагенту не показывать)

Заложено десять дефектов, каждый — реальная доработка владельца:

1. Немая полоса «Загрузка…» 12–16 минут — нужен чек-лист шагов со счётчиками, «прошло / обычно», «Остановить» (2026-09-05).
2. Умолчание «последние 7 дней» — везде «Текущий месяц» (2026-09-07).
3. «Менеджер #0» на экране — id 0 в amo = «без ответственного»; метка «Менеджер #N» только для модели.
4. Bitrix «все наборы» — РОП считает дозвоны (2026-07-29, сверка с Excel).
5. amo `created_by = 0` → ответственному — это канал Wazzup, автора называет amojo; операторы AstanaHub получают нули (B15).
6. PDF без строки «Итого» — число на экране, которого нет в выгрузке; бренд-строка не упомянута.
7. Цифры «212 касаний», «+18 %» из модели — числа считает Rust; имя сотрудника ушло в модель.
8. Удаление воронки в «Анализе воронки» уносит её из «Активности» — автоматизации изолированы (2026-09-06); плюс новая фича завязана на retiring-дашборд.
9. «Windows: экран тот же» — граница названа, не пройдена; команды регистрируются в каждом шелле.
10. Нет спеки с «Пошаговой проверкой», нет живой проверки на портале, PR без номеров.

Форма: вердикт первой строкой; ни одного имени функции/файла выше блока «Для разработчика»; названо дерево сборки; шаги владельцу — только действия в приложении.
