# Состав дистрибутива: какие паки каталога deploychan попадают в overlay/skills.
# Одно поле - текстовый пак, идентификатор из каталога.
# Три поля - софтовый пак: идентификатор, URL репозитория, закрепленная ревизия.
# Софтовая ветка еще не реализована, такие строки пока только разбираются.

comfyui-prompt-craft
comfyui-remote
obsidian-dataweave
openshell-pi-agent
telegram-custom-emoji-mosaic
x-content-advisor
xrayebator

# elevenlabs-living-voice не объявлен намеренно: версия из каталога ссылается на
# scripts/preflight.py, которого синк не привозит, а скилл из overlay/skills
# заменил бы одноименный апстримный целиком, вместе с этим скриптом.

# zaebal по спеку софтовый пак (клон и хуки в ~/.codex). Пока софтовая ветка не
# реализована, он объявлен текстовым: синк привозит только его инструкции.
zaebal
